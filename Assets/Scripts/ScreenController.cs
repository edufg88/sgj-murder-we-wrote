using UnityEngine;
using System.Collections;

public class ScreenController : MonoBehaviour {

	// Use this for initialization
	void Start () {
		Debug.Log("W: " + Camera.main.pixelWidth + ", H: " + Camera.main.pixelHeight);
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
